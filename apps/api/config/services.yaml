# yaml-language-server: $schema=../vendor/symfony/dependency-injection/Loader/schema/services.schema.json

# This file is the entry point to configure your own services.
# Files in the packages/ subdirectory configure your dependencies.
# See also https://symfony.com/doc/current/service_container/import.html

# Put parameters here that don't need to change on each machine where the app is deployed
# https://symfony.com/doc/current/best_practices.html#use-parameters-for-application-configuration
parameters:

services:
    # default configuration for services in *this* file
    _defaults:
        autowire: true      # Automatically injects dependencies in your services.
        autoconfigure: true # Automatically registers your services as commands, event subscribers, etc.
        bind:
            string $webAppUrl: '%env(WEB_APP_URL)%'

    # makes classes in src/ available to be used as services
    # this creates a service per class whose id is the fully-qualified class name
    App\:
        resource: '../src/'

    App\Domain\Chantier\Repository\ChantierRepository:
        class: App\Infrastructure\Persistence\Doctrine\Chantier\Repository\DoctrineChantierRepository

    App\Auth\Port\OAuthAccountStore:
        alias: App\Auth\Repository\OAuthAccountRepository

    App\Auth\Port\UserStore:
        alias: App\Infrastructure\Persistence\Doctrine\Auth\Repository\DoctrineUserRepository

    App\Auth\Port\GoogleUserResolver:
        alias: App\Auth\Infrastructure\KnpGoogleUserResolver

    App\Auth\Port\GoogleIdTokenVerifier:
        alias: App\Auth\Infrastructure\GoogleTokeninfoVerifier

    App\Auth\Infrastructure\GoogleTokeninfoVerifier:
        arguments:
            $allowedAudiencesCsv: '%env(GOOGLE_OAUTH_AUDIENCES)%'

    App\Auth\RegistrationPolicy:
        arguments:
            $selfServiceEnabled: '%env(bool:REGISTRATION_SELF_SERVICE_ENABLED)%'

    Aws\S3\S3Client:
        arguments:
            -   version: 'latest'
                region: 'auto'
                endpoint: '%env(R2_ENDPOINT)%'
                credentials:
                    key: '%env(R2_ACCESS_KEY_ID)%'
                    secret: '%env(R2_SECRET_ACCESS_KEY)%'

    App\Infrastructure\Storage\StorageAdapterInterface:
        class: App\Infrastructure\Storage\R2StorageAdapter
        arguments:
            $s3: '@Aws\S3\S3Client'
            $bucket: '%env(R2_BUCKET)%'
            $publicUrl: '%env(R2_PUBLIC_URL)%'

    # add more service definitions when explicit configuration is needed
    # please note that last definitions always *replace* previous ones
