framework:
    messenger:
        transports:
            async: '%env(MESSENGER_TRANSPORT_DSN)%'

        routing:
            App\Messenger\Message\GenerateThumbnailMessage: async

when@test:
    framework:
        messenger:
            transports:
                async: 'in-memory://'
