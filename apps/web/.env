# En dev : vide — le proxy Vite route /api et /auth vers localhost:8000
# En prod (Cloudflare Pages) : VITE_API_URL=https://cordeau-api.fly.dev
VITE_API_URL=

# Sentry — DSN à fournir dans .env.local
VITE_SENTRY_DSN=
VITE_SENTRY_ENVIRONMENT=development
